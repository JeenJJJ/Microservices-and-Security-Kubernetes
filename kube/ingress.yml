apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: journal-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: journalintime.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80